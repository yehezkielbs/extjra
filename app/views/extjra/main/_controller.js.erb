Ext.define('Extjra.controller.<%= @resources %>', {
    extend: 'Ext.app.Controller',

    models: [
        '<%= @model_name %>'
    ],

    stores: [
        '<%= @resources %>'
    ],

    views: [
        '<%= @resources %>'
    ],

    init: function() {
    }
});