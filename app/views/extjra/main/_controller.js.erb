Ext.define('Extjra.controller.<%= @abstract_model.resources %>', {
    extend: 'Ext.app.Controller',

    models: [
        '<%= @abstract_model.model %>'
    ],

    stores: [
        '<%= @abstract_model.resources %>'
    ],

    views: [
        '<%= @abstract_model.resources %>'
    ],

    init: function() {
    }
});